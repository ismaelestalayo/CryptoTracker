<UserControl
    x:Class="UWP.UserControls.PortfolioList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:helpers="using:UWP.Helpers"
    xmlns:models="using:UWP.Models"
    d:Height="200" d:Width="600"
    mc:Ignorable="d">

    <UserControl.Resources>
        <helpers:DateTimeOffsetShortener x:Key="DateOffsetShortener"/>
    </UserControl.Resources>

    <!-- ItemsSource="{x:Bind vm.Purchases, Mode=OneWay}" -->
    <ListView ItemsSource="{x:Bind Purchases, Mode=OneWay}" Margin="10 5">
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="models:PurchaseModel">
                <StackPanel Margin="0 10" BorderBrush="#888" BorderThickness="1" CornerRadius="6" Padding="15 5">
                    <TextBlock d:Text="Purchase - 01/01/2021" Opacity="0.5" FontWeight="Light">
                        <Run Text="{x:Bind Type}"/>
                        <Run Text="-"/>
                        <Run Text="{x:Bind Date, Converter={StaticResource DateOffsetShortener}}"/>
                    </TextBlock>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="25"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <Image Source="{x:Bind CryptoLogo}" d:Source="ms-appx:///Assets/Icons/iconNULL.png" Width="25"/>
                        
                        <StackPanel Grid.Column="1" Orientation="Vertical" Margin="5 0">
                            <TextBlock Text="Ethereum" FontWeight="Bold" Opacity="0.75" Margin="0 0 0 -3"/>
                            <TextBlock d:Text="0.12 XXX">
                                <Run Text="{x:Bind CryptoQty}"/>
                                <Run Text="{x:Bind Crypto}"/>
                            </TextBlock>
                        </StackPanel>

                        <StackPanel Grid.Column="2" Margin="10 0">
                            <TextBlock Text="Invested" Opacity="0.75" Margin="0 0 0 -5"/>
                            <TextBlock d:Text="51.3€" FontSize="16" HorizontalAlignment="Right">
                                <Run Text="{x:Bind InvestedQty}"/><!--
                                --><Run Text="{x:Bind CurrencySymbol}"/>
                            </TextBlock>
                        </StackPanel>

                        <StackPanel Grid.Column="3" Margin="10 0">
                            <TextBlock Text="Worth" Opacity="0.75" Margin="0 0 0 -5"/>
                            <TextBlock d:Text="62.5€" FontSize="16" HorizontalAlignment="Right">
                                <Run Text="{x:Bind Worth}"/><!--
                                --><Run Text="{x:Bind CurrencySymbol}"/>
                            </TextBlock>
                        </StackPanel>

                        <StackPanel Grid.Column="4" Margin="10 0">
                            <TextBlock Text="Delta" Opacity="0.75" Margin="0 0 0 -5"/>
                            <TextBlock d:Text="12%" FontSize="16" HorizontalAlignment="Right">
                                <Run Text="{x:Bind Worth}"/><!--
                                --><Run Text="{x:Bind CurrencySymbol}"/>
                            </TextBlock>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </DataTemplate>
        </ListView.ItemTemplate>

        <!-- For previsualization purposes -->
        <d:Rectangle/>
        <d:Rectangle/>
    </ListView>
    
    
</UserControl>
