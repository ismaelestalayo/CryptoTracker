<Page
    x:Class="CryptoTracker.Portfolio"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:controls2="using:Microsoft.UI.Xaml.Controls"
    xmlns:telerik="using:Telerik.UI.Xaml.Controls.Chart"
    NavigationCacheMode="Required"
    mc:Ignorable="d">

    <Page.Resources>
        <Style x:Key="DataGrid_HeaderStyle" TargetType="ContentControl">
            <Setter Property="FontWeight" Value="SemiLight" />
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContentControl">
                        <ContentPresenter Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}"
                            Background="#3F3F3F" CornerRadius="2 2 0 0"
                            ContentTransitions="{TemplateBinding ContentTransitions}"
                            HorizontalContentAlignment="Center"
                            VerticalContentAlignment="Center"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <MenuFlyout x:Key="PortfolioListFlyout">
            <MenuFlyoutItem Text="Edit" Click="RemovePortfolio_Click">
                <MenuFlyoutItem.Icon>
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE70F;" />
                </MenuFlyoutItem.Icon>
            </MenuFlyoutItem>
            <MenuFlyoutItem Text="Remove" Click="RemovePortfolio_Click">
                <MenuFlyoutItem.Icon>
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE74D;" />
                </MenuFlyoutItem.Icon>
            </MenuFlyoutItem>
        </MenuFlyout>
    </Page.Resources>

    <Grid x:Name="MainGrid" Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}" Padding="5 0">

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <!-- 
        ############################################################################################################### -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10 15">
                <ComboBox
                    x:Name="CryptoComboBox" Width="90" Margin="0 0 5 0"
                    PlaceholderText="Coin" Header="Crypto"/>

                <controls2:NumberBox 
                    x:Name="CryptoQtyNumberBox" Header="Amount"
                    Width="70px" Margin="0 0 5 0" AcceptsExpression="True"/>
                <controls2:NumberBox 
                    x:Name="InvestedQtyNumberBox" Header="Invested"
                    Width="70px" Margin="0 0 5 0" AcceptsExpression="True"/>

                <Button
                    Click="AddButton_Click" VerticalAlignment="Bottom" 
                    Background="#3F3F3F" Foreground="White" Margin="0 0 -10 0">
                    <SymbolIcon Symbol="Add"/>
                </Button>
            </StackPanel>
            
            <Grid x:Name="PortfolioChartGrid" Grid.Column="1" Background="Transparent" 
                  Height="33" VerticalAlignment="Bottom" Margin="5 15" CornerRadius="4">
            </Grid>
        </Grid>
        

        
        <!-- 
        ###############################################################################################################
        ###############################################################################################################
        ############################################################################################################### -->
        <ScrollViewer VerticalScrollBarVisibility="Visible" Grid.Row="1">
            <controls:DataGrid x:Name="DataGridd" Margin="7" ColumnHeaderStyle="{ThemeResource DataGrid_HeaderStyle}"
                VerticalAlignment="Top" HorizontalAlignment="Stretch"
                AlternatingRowBackground="Transparent" 
                AutoGenerateColumns="False"
                CanUserSortColumns="False" CanUserReorderColumns="False" CanUserResizeColumns="False"
                GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                RowDetailsVisibilityMode="Visible" SelectionMode="Single">
                <controls:DataGrid.Columns>
                    <controls:DataGridTemplateColumn Header="Crypto" Width="*">
                        <controls:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid VerticalAlignment="Center" HorizontalAlignment="Stretch" Padding="0 5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock VerticalAlignment="Center" HorizontalAlignment="Right">
                                        <Run Text="{Binding CryptoQty}"/>
                                        <Run Text="{Binding Crypto}"/>
                                    </TextBlock>

                                    <Image Grid.Column="1" Width="30" Margin="10 0" Source="{Binding CryptoLogo}"/>
                                </Grid>
                            </DataTemplate>
                        </controls:DataGridTemplateColumn.CellTemplate>
                    </controls:DataGridTemplateColumn>

                    <controls:DataGridTemplateColumn Header="Invested" Width="*">
                        <controls:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding InvestedQty}"/>
                                    <TextBlock Text="{Binding c}"/>
                                </StackPanel>
                            </DataTemplate>
                        </controls:DataGridTemplateColumn.CellTemplate>
                    </controls:DataGridTemplateColumn>

                    <controls:DataGridTemplateColumn Header="Worth" Width="*">
                        <controls:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding Worth}"/>
                                    <TextBlock Text="{Binding c}"/>
                                </StackPanel>
                            </DataTemplate>
                        </controls:DataGridTemplateColumn.CellTemplate>
                    </controls:DataGridTemplateColumn>

                    <controls:DataGridTemplateColumn Header="Currently" Width="*">
                        <controls:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <!--<TextBlock Text="{Binding arrow}"   Foreground="{Binding ProfitFG, Mode=TwoWay}"/>-->
                                    <TextBlock Text="{Binding Current}"/>
                                    <TextBlock Text="{Binding c}"      />
                                </StackPanel>
                            </DataTemplate>
                        </controls:DataGridTemplateColumn.CellTemplate>
                    </controls:DataGridTemplateColumn>

                    <!--<controls:DataGridTemplateColumn Header="Profit" Width="*">
                    <controls:DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock Text="{Binding Profit}"/>
                                <TextBlock Text="{Binding c}"/>
                            </StackPanel>
                        </DataTemplate>
                    </controls:DataGridTemplateColumn.CellTemplate>
                </controls:DataGridTemplateColumn>-->

                </controls:DataGrid.Columns>

                <controls:DataGrid.RowStyle>
                    <Style TargetType="controls:DataGridRow">
                        <Setter Property="controls:DataGridRow.ContextFlyout">
                            <Setter.Value>
                                <MenuFlyout>
                                    <!--<MenuFlyoutItem Text="Edit" Click="test">
                                        <MenuFlyoutItem.Icon>
                                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE70F;" />
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>-->
                                    <MenuFlyoutItem Text="Remove" Click="RemovePortfolio_Click">
                                        <MenuFlyoutItem.Icon>
                                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE74D;" />
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>
                                </MenuFlyout>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </controls:DataGrid.RowStyle>

                <controls:DataGrid.RowDetailsTemplate>
                    <DataTemplate>
                        <Grid Padding="0 15" CornerRadius="0 0 5 5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- First Column-->
                            <TextBlock Grid.Row="0" Grid.Column="0" Style="{ThemeResource ListViewTextBlock}" FontWeight="Medium" Margin="20 0 10 0" Text="Buy price: " HorizontalAlignment="Left"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Style="{ThemeResource ListViewTextBlock}" FontWeight="Light"  HorizontalAlignment="Right">
                            <Run Text="{Binding BoughtAt, Mode=OneWay}"/>
                            <Run Text="{Binding c, Mode=OneWay}"/>
                            </TextBlock>

                            <TextBlock Grid.Row="1" Grid.Column="0" Style="{ThemeResource ListViewTextBlock}" FontWeight="Medium" Margin="20 0 10 0" Text="Currently: " HorizontalAlignment="Left"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Style="{ThemeResource ListViewTextBlock}" Foreground="{Binding ProfitFG, Mode=OneWay}" FontWeight="Light" HorizontalAlignment="Right">
                            <Run Text="{Binding Current, Mode=OneWay}"/>
                            <Run Text="{Binding c, Mode=OneWay}"/>
                            </TextBlock>

                            <TextBlock Grid.Row="2" Grid.Column="0" Style="{ThemeResource ListViewTextBlock}" FontWeight="Medium" Margin="20 0 10 0" Text="Delta: " HorizontalAlignment="Left"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Style="{ThemeResource ListViewTextBlock}" Foreground="{Binding ProfitFG, Mode=OneWay}" FontWeight="Light" HorizontalAlignment="Right">
                            <Run Text="{Binding arrow, Mode=OneWay}"/>
                            <Run Text="{Binding Delta, Mode=OneWay}"/>
                            <Run Text="%"/>
                            </TextBlock>


                            <!-- Seconds Column -->
                            <TextBlock Grid.Row="0" Grid.Column="2" Style="{ThemeResource ListViewTextBlock}" FontWeight="Medium" Margin="30 0 10 0" Text="Cost: " HorizontalAlignment="Left"/>
                            <TextBlock Grid.Row="0" Grid.Column="3" Style="{ThemeResource ListViewTextBlock}" FontWeight="Light" HorizontalAlignment="Right">
                            <Run Text="{Binding InvestedQty, Mode=OneWay}"/>
                            <Run Text="{Binding c, Mode=OneWay}"/>
                            </TextBlock>

                            <TextBlock Grid.Row="1" Grid.Column="2" Style="{ThemeResource ListViewTextBlock}" FontWeight="Medium" Margin="30 0 10 0" Text="Worth: " HorizontalAlignment="Left"/>
                            <TextBlock Grid.Row="1" Grid.Column="3" Style="{ThemeResource ListViewTextBlock}" Foreground="{Binding ProfitFG, Mode=OneWay}" FontWeight="Light" HorizontalAlignment="Right">
                            <Run Text="{Binding Worth, Mode=OneWay}"/>
                            <Run Text="{Binding c, Mode=OneWay}"/>
                            </TextBlock>


                            <!-- Third Column -->
                            <TextBlock          Grid.Column="4" Style="{ThemeResource ListViewTextBlock}" Foreground="{ThemeResource ListViewItemSelectedForegroundThemeBrush}" FontWeight="Medium" Margin="30 0 0 0" Text="Purchased: " HorizontalAlignment="Left"/>
                            <CalendarDatePicker 
                                Grid.Column="5" Date="{Binding Date, Mode=TwoWay}" 
                                Margin="0 -2 0 0" VerticalContentAlignment="Stretch"
                                DateChanged="purchaseDate_changed"
                                DateFormat="{}{day.integer} {month.full} {year.full}"/>
                        </Grid>
                    </DataTemplate>
                </controls:DataGrid.RowDetailsTemplate>
            </controls:DataGrid>
        </ScrollViewer>
        
        
        
        
    </Grid>

</Page>
