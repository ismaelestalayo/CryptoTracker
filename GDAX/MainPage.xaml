<Page
    x:Class="GDAX.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GDAX"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

	<Page.Resources>
		<SolidColorBrush x:Key="CoinBase_Blue" Color="#0d6edd"/>
	</Page.Resources>
	
	<Grid >
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
		<!-- ThemeResource SystemControlPageBackgroundChromeLowBrush -->
		<RelativePanel Grid.Row="0" Background="{StaticResource CoinBase_Blue}">
			<TextBlock Text="Coinbase" Name="Title" 
					   Style="{ThemeResource HeaderTextBlockStyle}"
                       FontSize="42" Foreground="{ThemeResource ApplicationHeaderForegroundThemeBrush}"
					   Margin="20,20,20,10"
                       RelativePanel.AlignLeftWithPanel="True" />
			
			<Button FontFamily="Segoe MDL2 Assets" Content="&#xE115;" Click="SettingsButton"
					FontSize="32" Background="Transparent" BorderBrush="Transparent" 
					Foreground="{ThemeResource ApplicationHeaderForegroundThemeBrush}"
					Margin="20,20,20,10"
					RelativePanel.AlignRightWithPanel="True" RelativePanel.AlignBottomWith="Title"/>

		</RelativePanel>

		<Frame Name="MainFrame" Background="Gray" Grid.Row="1">

		</Frame>



	</Grid>

</Page>
